{
  "name": "@opencode-dispatch/cli",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "opencode-dispatch": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-dispatch/core": "workspace:*",
    "ink": "^5",
    "react": "^18"
  },
  "devDependencies": {
    "@types/react": "^18"
  }
}
